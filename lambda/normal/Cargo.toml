[package]
name = "lambda-normal"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
lambda-flows = "0.1"
serde_json = "1.0"
slack-flows = "0.1"
store-flows = "0.1"
